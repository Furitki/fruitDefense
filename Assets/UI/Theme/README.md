# Release runtime UI theme

The one authoritative release `RuntimeUiTheme` asset belongs here and is bound once through the persistent Bootstrap composition root.

The release theme serializes exactly one complete active production art set. The approved active direction is `sunny-orchard-painted@1`; no scene-local theme, route-specific art set, default-skin fallback, or reference-board dependency is allowed.
