## 1. Optical Resource Contract

- [x] 1.1 Add serialized per-binding optical alpha insets, validation, manifest/export ownership, and allocation-free destination-space resolution.
- [x] 1.2 Normalize the active painted action-surface family to one contained visible envelope while preserving source/runtime dimensions, slice borders, and Unity GUIDs.

## 2. Shared Typography and Composition

- [x] 2.1 Add validated role-level typography optical offsets and apply them through the shared cached GUI styles.
- [x] 2.2 Replace safe-inset icon proxies with binding optical bounds in shared action and metric composition, with no second geometry path.
- [x] 2.3 Add one reusable inline icon-label/indicator-copy resolver and use it for the paused Battle modal hint.

## 3. Cross-route Remediation

- [x] 3.1 Audit Bootstrap, Lobby, Battle, modal, and Settlement shared actions/titles/metrics and correct remaining paired-size or optical-alignment defects through shared anatomy.

## 4. Validation and Evidence

- [x] 4.1 Extend Editor resource and quality smoke coverage for exact alpha metadata, action-family envelopes, corrected typography, visual-group centering, containment, and supported scale variants.
- [x] 4.2 Run `FruitDefense.Editor.ProjectSetup.SmokeValidate` and focused UI suites; fix every failure attributable to this change.
- [x] 4.3 Build ordinary WebGL with `FruitDefense.Editor.WebBuild.Build`, capture canonical paused-modal and cross-route resting/pressed evidence, and record measured before/after results plus build identity.
