## ADDED Requirements

### Requirement: Eight-point Battle material Gate A
The eight-point polish SHALL publish a canonical ordinary-WebGL Battle Gate A from one identified acceptance payload at 402×874 full and representative inset safe areas, SHALL compare it beside the supplied reference and approved 7/10 baseline, and SHALL remain pending until the user explicitly reviews the fresh-green action family and generated page/container material integration.

#### Scenario: Canonical material comparison is captured
- **WHEN** the focused art checks pass and the new 402×874 ready state is captured from the rebuilt ordinary WebGL payload
- **THEN** the user handoff contains exactly one same-viewport, same-state three-column `reference / immutable user-scored before / new real-runtime after` comparison, while technical evidence records the payload, theme, ArtSet, role-font identities, generated-asset hashes, deterministic transforms, contrast measurements, and stable geometry

#### Scenario: Automated gates pass without user review
- **WHEN** deterministic export, Unity validation, WebGL build, containment, contrast, and interaction checks all pass
- **THEN** the change remains visually pending and SHALL NOT claim an `8/10` result until the user supplies an explicit score or approval

#### Scenario: Assistant prepares a candidate handoff
- **WHEN** a technically valid material candidate is ready for subjective review
- **THEN** the assistant SHALL NOT self-score, visually approve, or continue hidden aesthetic iteration, and intermediate generated assets, decomposition boards, and multi-state captures remain internal unless the user explicitly requests them

#### Scenario: Visual candidate is rejected
- **WHEN** the user finds the green, rim/shadow integration, paper materials, cards, slots, or full-page composition below the target
- **THEN** only the affected reviewed direct master is replaced and the same three-column Gate A is rerun without restoring sheet/procedural material paths or changing authoritative layout/input geometry

#### Scenario: Candidate drifts from an approved reference parameter
- **WHEN** comparison or measurement shows that a generic visual rule changed a reference-authoritative color, luminance, shape, rim, outline, shadow, texture, or proportion
- **THEN** the candidate fails Gate A regardless of its automated contrast result, the approved raster is restored, and any contrast correction is applied to the separate text/icon content token first
