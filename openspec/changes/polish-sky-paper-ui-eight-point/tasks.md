## 1. Approved Baseline and Source Contract

- [x] 1.1 Copy the approved 7/10 ready-state comparison, release theme/ArtSet identity, selected source hashes, and user ratings into this change's evidence area as the immutable before baseline.
- [x] 1.2 Inventory the selected fourteen semantic source/runtime paths, `.meta` GUIDs, slice/safe metadata, current procedural recipes, and rendered contrast so replacement and deletion can be proven without a fallback.

## 2. ImageGen Material Production

- [x] 2.1 Generate and select directly replaceable action-family masters with a fresher leaf-green primary/secondary direction and the accepted cream-rim, soil-outline, highlight, and short-shadow anatomy.
- [x] 2.2 Generate and select directly replaceable structural-family masters for the page shell, standard/raised panels, metric capsule, selectable/recipe card, tool slot, dashed nursery slot, and transparent-center soil-stage frame.
- [x] 2.3 Preserve the selected individual outputs in this change's evidence area with final prompts, output identities, SHA-256 hashes, intentional semantic reuse, and explicit text/icon-free ownership notes.

## 3. Deterministic Extraction and Integration

- [x] 3.1 Replace fixed-sheet extraction with hash-locked individual-master integration permitting only complete-component crop/alpha extraction, approved geometry-alpha masking, transparent padding, alpha-safe resize, measurement, hashing, and export.
- [x] 3.2 Replace the selected fourteen owned source/runtime PNGs in place, preserve destination `.meta` GUIDs and slicing, bump the single ArtSet/theme revision, and update manifest/provenance records.
- [x] 3.3 Delete superseded sheet bindings and selected procedural material recipes/fallback paths; retain no alternate generated or script-painted production source for those semantic slots.
- [x] 3.4 Extend validation for direct-asset hashes/transforms, alpha containment, stretch-safe centers, transparent stage center, text/icon-free rasters, fresh-green comparison, `4.5:1` content contrast, manifest completeness, and absence of selected sheet/procedural paths.

## 4. Validation and Battle Gate A

- [x] 4.1 Run the exporter twice from unchanged direct masters and prove stable source/runtime/manifest/ArtSet hashes plus stable GUID/import geometry.
- [x] 4.2 Run focused art/theme/layout tests and aggregate `FruitDefense.Editor.ProjectSetup.SmokeValidate`, fixing all blocking seams, contrast, alpha-edge, mixed-source, or containment defects.
- [x] 4.3 Build ordinary WebGL through `FruitDefense.Editor.WebBuild.Build` and capture same-payload 402×874 full and representative inset ready/active/paused/selected-detail evidence.
- [x] 4.4 Produce one canonical reference vs user-scored-before vs new-real-runtime comparison and record generated-asset, build, theme, ArtSet, font, geometry, contrast, and pointer identities in technical evidence.
- [ ] 4.5 Stop at the new Battle Gate A and obtain explicit user scoring/approval before claiming the 8-point target or synchronizing candidate-specific visual approval; the user-approved production workflow itself is already normative.

## 5. Rejected-candidate Correction

- [x] 5.1 Record the rejected runtime feedback and correct the semantic contract from universal warm-white action content to the approved light-green/soil-brown pair.
- [x] 5.2 Restore the accepted `action-green.png` direct master for primary/secondary, update exact theme/manifest/provenance identities, and prove `4.5:1` final-pixel contrast without material recoloring.
- [x] 5.3 Delete the four-segment action focus outline and update structural acceptance tests; the final marker-free interaction behavior is completed in 7.1.
- [x] 5.4 Make nine-slice patch coverage and sampling seam-safe across representative PC scale factors while preserving transparent-center stage geometry.
- [ ] 5.5 Repeat deterministic export, run focused and aggregate Unity validation, build PC and ordinary WebGL, and capture the corrected multi-resolution runtime evidence.
- [ ] 5.6 Stop at the corrected Battle Gate A for explicit user review; do not self-score the visual result.

## 6. Reference-Authority Recalibration

- [x] 6.1 Make explicitly supplied or user-approved reference-image visual parameters P0 authority, with generic visual guidance limited to no-reference work and parameters the reference leaves unspecified.
- [x] 6.2 Require content-token-first contrast correction and forbid darkening, recoloring, regenerating, overlaying, or substituting a reference-authoritative raster to satisfy a generic contrast pairing.
- [x] 6.3 Synchronize the active delta specification, stable UI visual-system document, source workflow, exporter failure guidance, and Unity validator remediation text without changing the approved light-green master.

## 7. Marker-free Slot Interaction Correction

- [x] 7.1 Remove the standard/compact action `marker.selected` interaction-cue implementation and replace it with restrained contained focus/press motion using existing theme tokens.
- [x] 7.2 Remove selected-marker and duplicate-indicator drawing from tool/nursery slots, preserve their reference raster color, and add a short selection pulse for empty and occupied nursery clicks without changing hit geometry.
- [x] 7.3 Trace the arrowed nursery lines to the reviewed direct master's solid outer rim and dashed inner rail rather than a runtime primitive or PC nine-slice seam; supersede the earlier keep-visible conclusion when the user rejects those pixels at P0.
- [x] 7.4 Update focused source/behavior tests, run strict OpenSpec validation and Unity focused/aggregate smoke, then rebuild ordinary WebGL for visual review.

## 8. Rejected Surface-Suppression Candidate

- [x] 8.1 Trace the annotated lines to their valid authored surface provenance and record that they are distinct from the fractional-scale PC seam.
- [x] 8.2 Produce the surface-suppression candidate while preserving resources and geometry; retain it only as rejected historical evidence.
- [x] 8.3 Record the user's rejection that the candidate removed required UI frames together with the defect.

## 9. Authored-Frame Restoration

- [x] 9.1 Correct the active contract and stable visual-system guidance so the light authored frames remain while only black/transparent PC seams are rejected.
- [x] 9.2 Restore the three Battle surface families, remove the rejected slot-surface suppression path, and retain marker-free contained motion plus the shared single-draw nine-slice fix.
- [x] 9.3 Run strict OpenSpec and focused Unity validation, rebuild PC and ordinary WebGL, and capture corrected evidence with the authored frames present and black seams absent.

## 10. Line-Free Nursery Carrier Correction

- [x] 10.1 Correct the active and stable contracts so the nursery section frame and each rounded-paper carrier remain, while the slot master contains no solid or dashed linear rail.
- [x] 10.2 Replace only the `slot.nursery` direct ImageGen master, preserve its approved alpha geometry through a hash-locked asset-specific mask, remove the obsolete dashed-rail production/validation path, and update provenance plus focused validators.
- [x] 10.3 Export deterministically twice, run strict OpenSpec and focused Unity validation, rebuild PC and ordinary WebGL, and capture the rail-free carrier at normal and clicked states for explicit user review.

## 11. PC Alpha-Fringe Correction

- [x] 11.1 Supersede the incomplete rail-only diagnosis: record that the first line-free RGB plate was combined with legacy alpha geometry, leaving mismatched neutral/dark and orange edge RGB that selected PC scales exposed as four perimeter lines.
- [x] 11.2 Replace the nursery direct master, remove the active legacy geometry mask, derive alpha from the new output's own connected neutral background, clear low-alpha ringing after every resize, and reject hidden RGB plus dark/neutral partial-alpha pixels.
- [x] 11.3 Export deterministically twice, run strict/focused validation, rebuild the Windows player, and obtain PC-package evidence or stop with the exact capture limitation without substituting WebGL evidence.

## 12. Metric Carrier and Global Transparent-Edge Contract

- [x] 12.1 Trace the remaining Header lines to `surface.metric` reusing the bordered `panel-paper.png` master plus missing low-alpha cleanup, and synchronize the active/stable contracts so Header/Nursery panels remain while metric/nursery carriers are line-free.
- [x] 12.2 Give `surface.metric` a dedicated reviewed master and same-output connected-background alpha; reject incompatible master sharing, hidden RGB, low-alpha ringing, line-free-carrier dark/neutral fringe, and continuous metric perimeter rails in exporter and Unity validation.
- [x] 12.3 Export deterministically twice, run strict OpenSpec plus focused/aggregate Unity validation, rebuild the Windows player, and record the exact pass/block/capture evidence without substituting ordinary WebGL for PC-package proof.
