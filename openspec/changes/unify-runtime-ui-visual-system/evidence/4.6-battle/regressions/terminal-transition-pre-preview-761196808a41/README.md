# Superseded terminal-transition capture attempt

The production `ConfigureAcceptanceFlow(victory)` command submits the result and
starts the Battle-to-Settlement transition in the same call. The bounded CDP
attempt therefore captured Settlement rather than a stable Battle terminal card.
These files are retained only to document why task 4.6 now uses the existing
URL-guarded Battle acceptance-state bridge for visual terminal previews. Actual
terminal submission and routing remain covered by task 5.3.

