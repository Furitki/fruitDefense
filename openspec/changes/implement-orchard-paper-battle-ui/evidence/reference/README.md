# Concept D reference

`concept-d-orchard-paper-battle.png` is the approved visual-direction reference generated with the built-in ImageGen tool. It is review-only evidence for hierarchy, material balance, spacing, and ornament restraint.

It is not a production master, contains non-authoritative generated text, and must never be referenced by a scene, presenter, `RuntimeUiTheme`, `RuntimeUiArtSet`, runtime `Resources`, or release build. Production UI remains separately composed from packaged text, semantic components, and owned text-free masters.
