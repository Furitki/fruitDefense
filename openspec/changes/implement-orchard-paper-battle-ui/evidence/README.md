# Acceptance evidence

- `reference/` contains the review-only ImageGen direction and is never a runtime dependency.
- `webgl-402x874-full/` contains canonical full-safe-area ready, active, paused, and selected-detail canvas captures plus the passing manifest.
- `webgl-402x874-inset-44-34/` contains canonical inset ready and selected-detail canvas captures plus the passing manifest.

Both WebGL manifests were captured from the same rebuilt ordinary-WebGL payload. They prove the shared UI contract only; they do not authorize Douyin or WeChat conversion.
