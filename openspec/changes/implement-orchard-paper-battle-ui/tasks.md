## 1. Gameplay-stage art contract

- [x] 1.1 Save the approved D concept as review-only change evidence and record that it is not a production dependency.
- [x] 1.2 Add `surface.gameplay-stage` as slot 56 with nine-slice geometry and update the finite runtime metadata/validator contract without renumbering existing slots.
- [x] 1.3 Create and review the standalone text-free gameplay-stage master, then extend both deterministic exporters, manifests, galleries, ArtSets, and importer metadata with stable bindings.
- [x] 1.4 Run both exporters twice and verify stable source/runtime hashes, transparent-center/slice safety, and byte-identical consecutive runtime output.

## 2. Battle hierarchy and layout

- [x] 2.1 Replace the legacy lower-page `BattleSurface` with a board-sized `BattleStage` that shares Header's horizontal track and uses the gameplay-stage surface.
- [x] 2.2 Replace separate ToolTray/Detail occupancy with one authoritative ContextTray whose tool and selected-detail anatomies are mutually exclusive.
- [x] 2.3 Reflow ContextTray, NurseryTray, and RefreshAction on the 4pt rhythm so the lower stack closes within 8–40 logical points of the reference safe-content edge.
- [x] 2.4 Preserve Primary/Secondary/Quiet action semantics, minimum touch targets, finite text owners, and one BattlefieldProjection for drawing and input.

## 3. Persistent validation

- [x] 3.1 Update Battle layout/quality smoke coverage for stage containment, shared track, context exclusivity, lower closeout, text fit, touch targets, and absence of the legacy enclosing panel.
- [x] 3.2 Update visual-system validation for the 56-slot contract, gameplay-stage source/runtime ownership, nine-slice/import metadata, and approved structural role wiring.
- [x] 3.3 Extend WebGL acceptance output with gameplay-stage/section bounds, outline bands, structural-frame count, context mode, refresh lower margin, and live text containment.

## 4. Verification and synchronization

- [x] 4.1 Run focused Battle layout, runtime UI quality, visual-system, and copy/content Editor suites to green.
- [x] 4.2 Run `FruitDefense.Editor.ProjectSetup.SmokeValidate` to green.
- [x] 4.3 Build ordinary WebGL with `FruitDefense.Editor.WebBuild.Build` and capture 402×874 full/inset ready, active, paused, and selected-detail evidence from the live canvas.
- [x] 4.4 Review the live screenshots against the D intent and numeric contract; close all blocking/high hierarchy, border, overflow, dead-space, mixed-set, or input defects.
- [x] 4.5 Synchronize the accepted structural-weight, ContextTray, stage-art, and review rules into `docs/ui/ui-visual-system.md` and the affected main specs without copying transient build evidence.
