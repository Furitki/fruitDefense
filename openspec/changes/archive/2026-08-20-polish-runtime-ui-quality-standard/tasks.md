## 1. Standard and baseline audit

- [x] 1.1 Freeze the current `sunny-orchard-painted@1` WebGL build as the 50-point baseline with representative full/inset screenshots, payload identity, and links to the approved style board and painted component proof.
- [x] 1.2 Write the normative UI quality checklist and numeric quality profile for typography roles, alignments, line policies, spacing, touch targets, color/contrast, icon optical boxes, nine-slice safety, illustration placement, and route hierarchy.
- [x] 1.3 Audit every required Bootstrap, Lobby, Battle, and Settlement state against the standard and publish a severity-ranked defect inventory with owner, authoritative rectangle/resource, evidence, and pass criteria.
- [x] 1.4 Inventory the active ArtSet's 49 bindings and all production/review resources, recording importer geometry, alpha bounds, optical metrics, ownership, references, and stale/unbound candidates.

## 2. Automated quality gates

- [x] 2.1 Add one editor-owned runtime UI quality profile consumed by tests for supported viewports/insets and numeric thresholds without duplicating values in presenters.
- [x] 2.2 Add a finite text inspection catalog that uses the packaged Noto Sans SC font and shared GUI styles to verify role, alignment, baseline, width, height, and allowed line count for all player-visible copies and states.
- [x] 2.3 Validate containment, required gaps, optical centering, repeated-row baselines, touch targets, and draw/hit authority for text, icons, indicators, metrics, actions, cards, status, modals, and results.
- [x] 2.4 Validate semantic token use and effective contrast for normal, selected, pressed, loading, disabled, success, warning, error, scrim, and modal states while requiring non-color state cues.
- [x] 2.5 Extend production resource validation for 49-slot ownership, importer geometry, alpha-edge contamination, nine-slice protected regions, optical icon outliers, illustration aspect, unbound files, review dependencies, and mixed-set references.
- [x] 2.6 Aggregate the new quality gates through `ProjectSetup.SmokeValidate` and `P0ValidationSuite.Run` without adding disposable daily-menu commands.

## 3. Shared component and route polish

- [x] 3.1 Fix shared typography, alignment, spacing, state, and color defects in `RuntimeUiGui` and theme usage while keeping cached styles and zero-allocation hot paths.
- [x] 3.2 Refine Lobby title, three level cards, thumbnails, selected/loading markers, text hierarchy, and primary action against the approved proof at all supported full/inset sizes; any audit-approved external rectangle change must update the single layout and hit authority together with no input drift.
- [x] 3.3 Refine Battle header metrics, dividers, status/wave action, tool/nursery slots, detail, pause, terminal banner/vista, and transient feedback without changing simulation or command order; any audit-approved chrome rectangle change must update draw and hit authority together while battlefield interaction geometry remains protected.
- [x] 3.4 Refine Bootstrap initialization/error and Settlement victory/defeat/metrics/Return/Retry composition, typography, indicator placement, and color hierarchy without changing route/session behavior; any audit-approved external rectangle change must update the single layout and hit authority together with no input drift.
- [x] 3.5 Correct production icon/indicator optical alignment, edge contamination, visual weight, and smallest-size legibility through reviewed masters and deterministic in-place exports that preserve GUIDs.
- [x] 3.6 Correct surface/ornament/illustration defects found by the audit, update manifests and review gallery deterministically, and keep all review-only assets outside release dependencies.

## 4. Verification and resource cleanup

- [x] 4.1 Run focused text/layout/color/resource gates, candidate preview/activation/Undo, `ProjectSetup.SmokeValidate`, deterministic/session tests, and the aggregate P0 suite with no generated fixture or scene/theme mutation.
- [x] 4.2 Build ordinary WebGL and run a 402×874 full/inset preflight that blocks on any high-severity alignment, overflow, contrast, seam, stretch, mixed-set, or input defect.
- [x] 4.3 After preflight passes, run the 360/375/402/430 full/inset Shell matrix and 402 full/inset Bootstrap/Battle/victory/defeat cross-route matrix with canonical manifests and manual original-resolution review.
- [x] 4.4 Remove temporary inspection helpers, capture servers/logs, generated invalid fixtures, rejected resource experiments, and unreferenced production artifacts while preserving canonical and useful regression evidence.
- [x] 4.5 Update the stable UI guide and resource READMEs with the final standard and authoritative workflow, publish the closed defect inventory and final quality audit, and keep ordinary WebGL distinct from mini-game platform evidence.

## 5. User-rejected visual alignment rework

- [x] 5.1 Reopen the visual audit from the actual 1280×720 browser host, withdraw the rectangle-only 100/100 conclusion, and publish redline evidence for host clipping, combined icon/text centering, battlefield gutters, repeated-row baselines, and route-level occupied-content balance.
- [x] 5.2 Add one project-owned WebGL template that uniformly contains the complete portrait canvas on desktop and embedded windows, preserves aspect ratio and pointer mapping, removes scroll-dependent access, and is selected by the stable WebGL build workflow rather than patched into build output.
- [x] 5.3 Extend automated host acceptance for 1280×720, 1440×900, and one narrower desktop window, asserting DOM canvas containment, no page scrollbars, uniform scale, identical payload identity, and correct canvas-relative Lobby input.
- [x] 5.4 Extend shared component anatomy and quality gates so every icon-plus-label action and repeated metric/result row is evaluated and rendered as one centered visual group with consistent baselines and no border-straddling icons.
- [x] 5.5 Recompose Lobby card internals and page rhythm from the redline: balance thumbnail and copy weight, vertically align title/body groups, place selected state without overloading the thumbnail, localize the start copy, and reduce accidental lower dead space with draw/hit authority unchanged or updated together.
- [x] 5.6 Recompose Battle header, battlefield projection, status gap, tool/nursery headings, and terminal content so visual gutters, repeated baselines, and group centers are balanced while grid coordinates, simulation, command order, and draw/hit projection remain identical authorities.
- [x] 5.7 Recompose Settlement result metrics, vista, banner, action groups, and page rhythm so icons stay inside rows, icon-plus-label actions are centered, the terminal separator has a semantic role, and the lower canvas no longer appears unfinished.
- [x] 5.8 Run direct visual-balance/layout/host gates, ProjectSetup aggregate, deterministic/session tests, candidate preview/activation/Undo, and the unique P0 suite with no scene/theme/ArtSet or fixture mutation.
- [x] 5.9 Build ordinary WebGL once and require a green 1280×720 host preflight before rerunning 360/375/402/430 full/inset Shell plus 402 full/inset Bootstrap/Battle/victory/defeat evidence; reject any mathematically valid but visibly off-center composition.
- [x] 5.10 Replace the invalidated final-quality audit with user-review-ready before/after evidence and summarize the durable UI rules for host scaling, visual groups, baselines, gutters, whitespace, route hierarchy, and resource replacement without claiming an aesthetic score on the user's behalf.
- [x] 5.11 Make the stable localhost WebGL server invalidate cached content hashes when files are replaced in place, verify conditional GET returns the new ETag/body after an ordinary rebuild-style replacement, and confirm the user-facing 4173 page loads the final contain host without a cache-busting query.
