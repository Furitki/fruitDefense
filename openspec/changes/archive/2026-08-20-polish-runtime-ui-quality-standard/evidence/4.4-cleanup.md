# Task 4.4 cleanup and local release-service audit

Status: **complete** on 2026-08-20.

## Removed

- 34 acceptance-owned `server.stdout.log` / `server.stderr.log` files emitted
  during the task 4.2/4.3 random-port runs. They contained no canonical state,
  route identity, or payload facts; those facts remain in the manifests. These
  logs are reproducible by running the acceptance script again.
- `resource-polish/icon-control-speed-runtime-preview.png`, an unreferenced
  96 x 96 single-icon preview, SHA-256
  `2AFC8662595E068EC129A17D2ECD161304C21136769D52334AA8A44628F0E31B`.
  The reviewed icon remains in `resource-polish-review.png` and in the
  deterministic production source/export. The exact orphan preview encoding is
  intentionally not retained.
- `resource-polish/indicator-drag-legal-runtime-preview.png`, an unreferenced
  96 x 96 duplicate of the current production runtime PNG, SHA-256
  `A504B653DBF19F1A468EEF3BB073DE990681E11E4CFBC0FC1201554F5EF07A04`.
  It is byte-recoverable from
  `Assets/UI/Art/Runtime/sunny-orchard-painted/icons/indicator-drag-legal.png`.

No `GeneratedInvalid*` fixture, Bootstrap probe, partial screenshot, capture
server helper, temporary importer, or rejected production candidate remained in
the repository at cleanup time. `RuntimeUiVisualSystemSmoke` still creates its
invalid-path asset only inside a guarded test and deletes it in `finally`; the
fixture directory contains only its stable README.

## Deliberately retained

- All task 4.2/4.3 canonical manifests and 83 canonical PNGs.
- The first Shell transition timing regression, its two valid pre-transition
  images, and its README. It documents a fail-closed acceptance race and the
  bounded structural rerun.
- The WebGL build log, resource inventory JSON/log/gallery, selected imagegen
  raw output and provenance, before/after audits, deterministic export record,
  old inactive-set remediation evidence, and review montages. Each is linked by
  its owning evidence README or records defect closure/reproducibility.
- `resource-inventory/build_inventory.py`: it is the referenced reproducible
  collector for the machine-readable inventory, not a runtime or disposable
  Editor helper.
- The user-facing `Fruit Defense/UI/Visual System` window and aggregate Editor
  tests. They are the stable authoring/validation workflow required by the
  change, not one-shot menu commands.

The resource inventory and release validator report zero missing/unbound
production PNGs, zero unclassified production ancillary files, and zero
unexpected mixed-set bindings. Rejected image-generation iterations are not
stored in the repository or referenced by the release.

## Process and profile audit

- Unity processes: `0`
- Chrome processes: `0`
- Random acceptance servers/listeners: `0`
- Current-change `server.stdout.log` / `server.stderr.log`: `0`
- Current-change residual `fruit-defense-cdp-*` profiles: `0`

Older global temporary CDP profiles and Unity logs predate this change and are
outside its ownership; they were not used by the final matrix or deleted here.

## `4173` release service

The existing listener remains PID `15688`; it was not stopped or restarted.
HTTP byte comparison proves it is serving the current `Builds/WebGL` tree:

| Resource | Local SHA-256 | HTTP `4173` SHA-256 | Result |
|---|---|---|---|
| `index.html` | `931B2343B58589CEE6CC12E1398BF03E0E2B7E6BD46D74632100ED8EA8AF6E13` | same | pass |
| `Build/WebGL.loader.js` | `34035C24F91B91E9E6AEB447743A0031D761F433D034614758E6F0CD4E65519F` | same | pass |

The served index declares the exact accepted payload versions:

- loader `34035c24f91b`
- data `1ca6385231e1`
- framework `2311a6f94ac3`
- wasm `ba2503f395b7`

Therefore no service switch is required; refreshing the existing page is
sufficient. This remains ordinary WebGL evidence only.

