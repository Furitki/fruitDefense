## 1. Freeze the approved baseline

- [x] 1.1 Record immutable 402×874 before screenshots for Home and claimable Activity and verify the approved reference files remain review-only dependencies.
- [x] 1.2 Add layout assertions that freeze the existing Home level/Start, Activity Claim, and primary navigation hit rectangles while permitting new contained visual child rectangles.

## 2. Complete the Activity illustration contract

- [x] 2.1 Add `illustration.hub-activity-reward` to the finite runtime ArtSet registry and update completeness, semantic ID, geometry, manifest, and validation coverage to 60 slots.
- [x] 2.2 Generate and inspect one text-free transparent starter-supply hero illustration, save the owned source with prompt/output hash, and export it deterministically to the stable runtime path.
- [x] 2.3 Bind the new illustration once in the active production ArtSet and pass importer, alpha-edge, ownership, and complete-set validation without a fallback path.
- [x] 2.4 Intermediate pass: replace `action.primary` in place with a text-free capsule master, preserve its stable slot/GUID, and validate the inverse outlined live-label treatment without a play glyph; superseded by task 5.10 after user review.
- [x] 2.5 Rebuild the shared bottom navigation as one paper tray with an upward-rising sun-paper selected tab, larger aligned icon/label groups, three reference-led low-detail domain silhouettes readable at 24–33 logical points, stable icon color in selected state, and no compact-action surface reuse.

## 3. Restore the Home page

- [x] 3.1 Recompose the three existing level cards so their real orchard illustrations dominate the left side while selection, copy, motion, and hit rectangles remain authoritative and contained.
- [x] 3.2 Rebuild the pre-battle growth preview with paper/ribbon hierarchy, a graphical growth cue, finite policy/effect lines, explicit state, and the existing large Start action.
- [x] 3.3 Extend focused Home layout, text, state, draw/hit parity, and 360/375/402/430 full+inset validation.

## 4. Restore the Activity page

- [x] 4.1 Replace the generic Activity host with the reference order: title/description, dominant framed reward hero, two icon-plus-label reward tiles, explicit claim state, and the existing large Claim action.
- [x] 4.2 Preserve actual reward/catalog copy, claim serialization, committed balance behavior, and claimable/claiming/claimed/locked/error action states without simulated reference data.
- [x] 4.3 Extend focused Activity layout, finite copy, state, draw/hit parity, and 360/375/402/430 full+inset validation.

## 5. Validate and synchronize

- [x] 5.1 Run Hub page smoke, runtime UI quality/resource smoke, aggregate `ProjectSetup.SmokeValidate`, and `git diff --check`; resolve every scoped failure after the navigation-icon correction.
- [x] 5.2 Build ordinary WebGL and capture Home plus claimable/claimed Activity at 402×874 from the real canvas with input telemetry after the navigation-icon correction.
- [x] 5.3 Produce same-state `reference / before / after` comparisons using the latest user-rejected navigation-icon build as the immutable before, and leave the visual gate pending until explicit user approval.
- [x] 5.4 After approval, synchronize the finite 60-slot contract and Home/Activity stable anatomy into the owning UI visual-system/current specs without copying transient build evidence.
- [x] 5.5 Correct the visual contract around the reference's simplicity, finite silhouette budget, and dedicated bottom-navigation base art; remove generic-panel navigation chrome, register the two owned navigation surfaces, and rerun the scoped release gates.
- [x] 5.6 Replace the near-black Home/Activity primary-action label outline with the shared warm soil-brown outline token, forbid pure-black and near-black UI outlines in the stable contract, and rerun editor plus real-canvas visual acceptance.
- [x] 5.7 Intermediate pass: restore `action.primary` to a light fresh lime-green material through one hash-locked ImageGen edit while preserving the then-current capsule silhouette, live label, stable path/GUID, and hit geometry; superseded by task 5.10.
- [x] 5.8 Replace both coarse script-authored Hub navigation masters with separately hash-locked refined ImageGen rasters, preserve the two-silhouette composition, and delete their procedural authoring functions.
- [x] 5.9 Add a release gate that forbids UI-art scripts from drawing or synthesizing visible pixels, synchronize the stable/current contracts, and rerun exporter, release validator, UI/Hub editor smokes, ordinary WebGL, and 402×874 Home/Activity capture.
- [x] 5.10 Restore `action.primary` from the user-selected original light-green rounded-square PNG, return its nine-slice border to 32, remove the capsule export dependency/composition path, preserve labels/layout/navigation, and rerun the focused release gates plus real-canvas capture.
- [x] 5.11 Freeze the user-rejected `20260902-final` Home canvas as the new immutable before, measure the approved Home reference at 402×874, and record the component distribution plus authorized component-level extraction boxes.
- [x] 5.12 Replace the three Home level-illustration masters with text-free complete image-window crops from the approved Home reference, remove the Home illustration-frame overlay, and prove every illustration stays contained during motion.
- [x] 5.13 Replace the selected-card, bottom-navigation base/tab, and three domain-icon masters with separately owned reference-derived fixed rasters; no visible asset pixel may come from a script-drawn border, primitive, recolor, or reconstruction path.
- [x] 5.14 Extend Home/navigation release validation for zero illustration overflow, single-outline cards, exact-size crisp icon review, component provenance, and 360/375/402/430 full+inset geometry.
- [x] 5.15 Run exporter, focused editor smokes, ordinary WebGL build/capture, strict OpenSpec validation, and deliver one 402×874 `reference / before / after` Home comparison with the visual gate pending user approval.
